#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10
 *
 * Description: prints base 10 digits
 *
 * Return: return 0
 */
int main(void)
{
	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	printf("\n");
	return (0);
}
